export enum STORES {
  NODE_STORE = 'nodeStore',
  DMAP_STORE = 'dmapStore',
  VERIFY_STORE = 'verifyStore',
}

export enum PAGE_PATHS {
  NODE = '/node',
  DMAP = '/dmap',
  COUNT = '/count',
  DATA = '/data',
}
